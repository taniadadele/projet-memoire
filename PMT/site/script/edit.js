//---------------------------------------------------------------------------
function ajoutsmile(smile)
{
//	document.formulaire.texte.value = document.formulaire.texte.value+' '+smile;
	document.getElementById("formulaire").texte.value = document.getElementById("formulaire").texte.value+' '+smile;
}
//---------------------------------------------------------------------------
